Flask
flask-socketio
Werkzeug
psycopg[binary,pool]

# notifiche push
pywebpush
cryptography

# server di produzione: gevent + worker WebSocket
gunicorn
gevent
gevent-websocket
# gunicorn.workers.ggevent importa packaging ma non lo dichiara fra le
# proprie dipendenze: senza questa riga il worker WebSocket non si
# carica e il deploy muore con
#   class uri 'geventwebsocket.gunicorn.workers.GeventWebSocketWorker'
#   invalid or not found: ModuleNotFoundError: No module named 'packaging'
# Finora arrivava di rimbalzo da qualche altro pacchetto, poi ha smesso.
packaging

# Mettiamo anche Resend API per la ver email
# Sarà solo temporaneo, nulla di così speciale
resend
resend==2.37.0
